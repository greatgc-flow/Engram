# Gemini CLI — Project Instructions
> This file is a **pointer only**. All normative content lives in `_sys/docs-v2/` (SSOT).
> As an AI peer, you MUST read the SSOT immediately upon startup to understand your operating rules.

## 1. Startup Requirements
1. **MANDATORY**: Read `_sys/docs-v2/MOC.md` to locate the rules for your current task.
2. **MANDATORY**: Read `_sys/docs-v2/10-invariants.md` for the non-negotiable hard rules.
3. **MANDATORY**: Read `_sys/docs-v2/specific/gc.md` and `_sys/docs-v2/specific/ag.md` for your specific peer configuration.
4. **MANDATORY**: Read `.ai/sessions/room-{uuid}/handoff.md` to synchronize with the current session state.

## 2. Directory Mappings
- Your local settings and memory are located in `_sys/gemini/config/` (or `_sys/antigravity/config/`).
