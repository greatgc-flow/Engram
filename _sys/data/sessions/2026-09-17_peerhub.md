# Session Archive — 2026-09-17 (R4/P4b design dialectic, cx rate-limit)

## R4/P4b architecture convergence — 2-voice design drafted, pending cx

Started the required 2+-model dialectic for R4 (P4b governance convergence + typed call), the last major item on the ratified renewal roadmap, blocked until D1 (D-CTX) closed on 2026-09-16.

- Dispatched independent research to ag.deepthink and cx.deepthink in parallel (neither saw the other's answer). ag.deepthink completed and produced a proposal: elevate the already-shipped `verify_credential_for_actor` into an `ApplicationAPI.submit()`-level `GovernanceAuthorizer`; migrate CLI call sites domain-by-domain (`operational-error` first, `consensus` last); remove duplicate identity defaults during migration; flagged its own uncertainty about human/local CLI usage without a D-CTX credential.
- cx.deepthink hit a **real, confirmed vendor usage limit** mid-task (reset 2026-09-19T17:16 — visible in `_sys/codex/health.json`'s `rate_limit_state`, not a false positive). Retried with cx.effort (a different, then-healthy profile) as a legitimate root-caused workaround, not a blind retry — it also hit the same real limit seconds later, confirming this is an account-level cap, not tier-specific. cx.standard/astra remained healthy but weren't used for this hard-reasoning task (quality-over-speed policy). No reset-credit ticket was available.
- User decision: cc stands in as the second independent voice now (precedented by this project's own D0 review), but **DIR-006 final ratification is explicitly held until cx reviews the design** once its rate limit clears.
- cc wrote an independent critique (`docs/design/peerhub-r4-p4b-independent-review-cc-2026-09-17.md`): agreed with ag's core mechanism and migration order, but corrected the "exact grant" framing — the gateway must preserve the asserted/verified duality already established in the original D-CTX proposal (Increment 2/3 text explicitly defers "whether asserted mutations remain supported" as its own later decision), not require a credential for every mutation (which would break all human-driven local CLI usage, since a human has no dispatch-admission credential to present). Also flagged that removing duplicate identity defaults (e.g. `proposal-vote --voter` defaulting to `"cc"`) would break an already-shipped, already-tested, already-released (v0.4.0) compatibility guarantee, and must be a separately-gated later step. Proposed tracking routing and verification-enforcement as two independent per-command facts in the production call-map.
- ag reviewed the critique and agreed with all three corrections, proposing a concrete `authorizer_behavior` enum (`ASSERTED_ONLY`/`VERIFIED_WHEN_PRESENTED`/`VERIFIED_REQUIRED`/`NOT_APPLICABLE`) for the call-map schema.
- Synthesized into `docs/design/peerhub-r4-p4b-converged-design-2026-09-17.md` — explicitly marked DRAFT, NOT DIR-006 ratified. Implementation is blocked until cx's independent review and unanimous agreement are recorded.

All 3 docs committed and pushed (`ef2a467`, `abeae8d`, `058ad13`), CI green. No code changes this session — design-only, per the ratified doc's own "this dispatch authorizes and performs no implementation" convention for architecture-decision rounds.

**Next**: wait for cx's quota to reset (~2026-09-19 17:16) and get its independent review before any R4 implementation starts. In the meantime, doing a folder-taxonomy survey (temp/permanent/workspace folder usage across peerhub and Engram) per user request.
