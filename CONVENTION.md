# Portable Dev Environment — Coding Conventions
> This file is a **pointer only**. All normative content lives in `_sys/docs-v2/` (SSOT).

All source code must comply with the rules defined in:
**`_sys/docs-v2/ops/conventions.md`**

## 1. Language Policy (CRITICAL)
- All agent definitions, skill files, policy documents, JSON artifacts, and system code MUST be in **English only**.
- See `_sys/docs-v2/10-invariants.md` (INV-19).

## 2. Shell & File Parsing
- Detailed rules for `.bat` and `.ps1` execution, JSON parsing, testing environments, and Hub script protection are defined in `_sys/docs-v2/ops/conventions.md`.
