# Session Archive — 2026-09-16 (PeerHub D-CTX closure + D:/P: drive cleanup)

## PeerHub: D-CTX Increment 1 fully closed

Continuing from D0→D7→Q2/D2→Increment 0→D5/D6 (shipped 2026-09-14), three real remaining gaps were found and closed today:

1. **Legacy `proposal-vote` had zero credential verification** — `ProposalCoordinator.add_proposal()`/`vote_proposal()` now forward `verified_required`/`credential_id` into the already-wired `ConsensusService`. CLI gained `--verified-required`/`--credential-id` on `proposal-add`/`proposal-vote`. 3 new e2e tests. Commit `ee329ab`.
2. **Identity flags weren't optional even with a valid credential** — new `resolve_actor_for_credential()` primitive (read-side counterpart to `verify_credential_for_actor`) lets `consensus vote --actor` and `proposal-vote --voter` be omitted when a valid `--credential-id` is presented; explicit+credential-together still enforces the existing equality check (contradiction still rejected). Commit `2670b10`.
3. **Live Carrier Matrix proof never run** — real `peerhub ask ag/cx` dispatches (user-approved, quota-consuming) proved `PEERHUB_CONTEXT_FILE` reaches the real child process with a fresh unpredictable value per attempt. Codex declined to echo the file's `credential_id` field twice (a model-safety false-positive, not a carrier defect) but confirmed the env var/path both times. Findings: `docs/design/peerhub-dctx-carrier-matrix-verification-2026-09-16.md`. Commit `00b195b`.

Also: **R0** (release-tag-vs-package-version CI guard, broken by P8's dynamic-version change) was found already fixed same-day as ratification (`a92c6e6`, 09-13) but never marked closed in the roadmap doc — closed the documentation gap, commit `4fa7831`.

Process note: 2 `hub.py ask --to ag.effort` dispatches were killed by the OS's own low-memory watchdog (~3.4GB/16GB free, not ag-specific — confirmed via `Get-Process`). After 2 kills, implemented the well-scoped piece (item 1) directly rather than retrying blind a third time.

Full suite 1699 passed, 0 pyright errors, CI green on every push. D-CTX Increment 1 (credential carrier + verified consensus path) is now complete end-to-end. Increment 2/3 remain deliberately unstarted future work; R4 (P4b governance convergence) is blocked on cx quota recovery for its required 2+-model dialectic round.

## D:\ and P:\ drive MECE cleanup

User reconfirmed the full standing operating policy set today (quota pacing, failure-recovery/stage-gate, design-discipline, terminal-delegation, plus new: resource-monitoring-per-work-unit, memory-file-size-management) — all saved/updated in the memory system; `MEMORY.md` split into an active index (8-9KB) + `MEMORY_ARCHIVE.md` for pre-09-01 entries.

**D:\ top-level**: surveyed (delegated to ag.effort), then deleted on user confirmation: `ClaudeDev` (2.65GB), `t2` (3.33GB), `tttt` (3.51GB), `.tmp.driveupload` (0B), `Engram&Peerhub.zip` (4.15GB), `Engram&Peerhub_20260809.zip` (2.53GB) — ~16.2GB reclaimed. `.Cache` (172GB, active Google Drive cache) and `Engram&Peerhub` (live P:\ backing folder) correctly left untouched.

**`Engram&Peerhub\` internals**: `workspace`/`workspace_ori` contained real uncommitted work (MarkItDown-Flow UI +206 lines, an experimental image-router change) and one real security finding (a plaintext Azure Document Intelligence API key in `workspace_ori`, never committed/pushed anywhere). Both changes archived to new GitHub branches (`archive/setup-modal-ui-wip-20260916`, `archive/image-router-v2-wip-from-workspace-ori-20260916`) off `greatgc-flow/MarkItDown-Flow.git` without touching `main`, independently re-verified via `gh api` (exact file lists, no credential files, `main` SHA unchanged) before deleting both folders. `engram-main-worktree` confirmed live/registered, left untouched. `Docs` explicitly left for the user to handle later.

Discovered mid-task: a 2026-09-12 planning doc (`2026-09-12_d-drive-mece-cleanup-plan.md`, written by a different AI, previously untracked in git) had independently reached matching conclusions 4 days earlier. Annotated it with today's execution outcome and added it to git tracking.

**P:\`_sys\data\temp\`** (the machine's OS TEMP dir, redirected onto the portable drive): 632 top-level items deleted via a reviewed PowerShell script (24h-age cutoff + explicit protection for the 2 live Claude session dirs and 3 named live-state files), reclaiming 3.24GB with 0 errors. `_sys/data/sessions/holding/2026-09-09_audit_scratch/` (2.8MB, findings already committed elsewhere) also removed.

`.gitignore` cleaned (2 duplicate `node_modules` lines removed; `.peerhub/`/`.ais/`/`.pnpm-store/` added — live SQLite DB, session transcripts, and pnpm cache respectively, none of which belong in P:'s history). Every deletion in this entire arc was independently re-verified by the terminal (`Test-Path`, `gh api` commit contents, free-space deltas) rather than trusted from peer reports alone.

Flagged, not touched: `.ais/` (382MB, real session/transcript data), `.peerhub/` (live DB, do not delete), `.claude.json.bak-20260812` (43KB, month-old config backup), `.peerhub/peerhub.sqlite3.pre-cleanup-backup-20260914`.
